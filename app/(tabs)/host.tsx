import { Text, View } from "react-native"

export default function HostScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Host screen (native placeholder)</Text>
    </View>
  )
}